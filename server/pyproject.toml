[project]
name = "iot-monitor-server"
version = "0.1.0"
description = "FastAPI + SQLite サーバー（Pi 4 上で稼働）"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.29.0",
    "pydantic>=2.7.0",
]
